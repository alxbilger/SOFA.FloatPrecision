Tools to generate a SOFA plugin containing all the components from `Sofa.Component` but instantiated with your custom float precision.

Usage:

```python
python plugin_maker.py float
```

This command will generate the sources of a plugin named `Sofa.Component.float` in the current directory. You can then compile it.